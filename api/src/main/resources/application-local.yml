spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/tikitaka}
    username: ${DATABASE_USERNAME:tikitaka}
    password: ${DATABASE_PASSWORD:tikitaka}
