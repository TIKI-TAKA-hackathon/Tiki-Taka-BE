spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

aws:
  s3:
    bucket: test-bucket

app:
  cors:
    allowed-origins: http://localhost:5173
  media:
    max-image-bytes: 1024
    upload-url-ttl-seconds: 300
    view-url-ttl-seconds: 300
