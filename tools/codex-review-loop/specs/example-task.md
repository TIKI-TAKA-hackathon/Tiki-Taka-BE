# Task: <one-line summary>

## Context
<what part of the system this touches, and why the change is needed>

## Requirements
- <explicit, testable requirement>
- <explicit, testable requirement>

## Out of scope
- <things NOT to touch — keeps the change surgical and the review focused>

## Done when
- <observable condition, e.g. "GET /api/orders/{id} returns 404 for an unknown id">
- All verification gates pass.
